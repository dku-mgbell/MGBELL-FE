import { styles } from './styles.css';

export default function Home() {
  return (
    <section className={styles.container}>
      <header className={styles.header}>위치를 설정해주세요!</header>
    </section>
  );
}
